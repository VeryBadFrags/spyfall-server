{
  "name": "online-spy",
  "version": "1.0.0",
  "description": "Back-end for the Online Spy game",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc && NODE_ENV=dev nodemon -w",
    "build": "tsc",
    "start": "npm run build && node dist"
    // "test": "eslint src"
  },
  "keywords": [],
  "author": "VeryBadFrags",
  "license": "MIT",
  "dependencies": {
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "typescript": "^4.9.4"
  }
}
